name: Test Workflow

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run test step
        run: echo "Hello, world!"
      
      - name: Notify user
        run: echo "Notification for 22f3002771@ds.study.iitm.ac.in"
